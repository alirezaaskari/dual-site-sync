<?php
/**
 * نمونه‌ی تنظیمات Dual Site Sync برای wp-config.php
 *
 * این بلوک را در wp-config.php هر دو سایت، *بالای* خط زیر قرار دهید:
 *
 *     /* That's all, stop editing! Happy publishing. *\/
 *
 * ⚠️ این فایل فقط نمونه است؛ آن را روی سرور آپلود نکنید.
 */

/* -------------------------------------------------------------------------
 * ۱) آدرس دو فروشگاه — در هر دو سایت دقیقاً یکسان
 * ---------------------------------------------------------------------- */

define( 'DSS_SITE_A_URL',   'https://samehrstore.ir' );
define( 'DSS_SITE_A_LABEL', 'سامهر استور' );

define( 'DSS_SITE_B_URL',   'https://romegacollection.com' );
define( 'DSS_SITE_B_LABEL', 'رومگا کالکشن' );

/* -------------------------------------------------------------------------
 * ۲) کلید رمز مشترک — در هر دو سایت باید *دقیقاً یکی* باشد
 *
 * این کلید هرگز روی شبکه منتقل نمی‌شود؛ فقط برای امضای HMAC هر درخواست
 * استفاده می‌شود. یک رشته‌ی تصادفی حداقل ۶۴ کاراکتری بسازید، مثلاً با:
 *
 *     openssl rand -hex 32
 *
 * یا از تولیدکننده‌ی کلید وردپرس: https://api.wordpress.org/secret-key/1.1/salt/
 * ---------------------------------------------------------------------- */

define( 'DSS_SHARED_SECRET', 'اینجا-یک-رشته-تصادفی-۶۴-کاراکتری-بگذارید' );

/* -------------------------------------------------------------------------
 * ۳) اختیاری — تعیین دستی هویت سایت
 *
 * به‌صورت پیش‌فرض هر سایت از روی دامنه‌ی خودش تشخیص می‌دهد که site_a است یا
 * site_b. اگر روی استیجینگ یا دامنه‌ی موقت کار می‌کنید، این را دستی تعیین کنید:
 * ---------------------------------------------------------------------- */

// define( 'DSS_SITE_KEY', 'site_a' );   // در سایت اول
// define( 'DSS_SITE_KEY', 'site_b' );   // در سایت دوم
